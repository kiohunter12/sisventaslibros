server:
  port: 7070


spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/TheReeds/Sistema-Venta-Libros.git #https://github.com/Yessenia1/ventalibros.git  ruta del repositorio git
          searchPaths: config-data
          #username:
          #password:
          default-label: main
  security:
    user:
      password: 123456
      name: root